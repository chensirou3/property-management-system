# 物业管理系统便携交付版 v0.1.4

版本标识：`v0.1.4-portable-delivery.1`

发布日期：2026-09-03

## 本版本解决的问题

- 项目运行数据只使用 Docker 命名卷，不依赖接收电脑的 C、D、E 等固定盘符。
- 新增 `检查运行环境.cmd`，在构建前检查 Docker Desktop 数据目录是否是指向不存在磁盘的失效链接。
- 启动前检查 Compose 是否出现主机绑定目录，防止后续修改重新引入固定盘符依赖。
- 检查 `.env` 是否缺少密钥、仍使用示例占位符、密钥重复或端口无效。
- 检查四个默认/自定义端口是否被其他程序占用。
- API 和 Web 健康检查改为读取 `.env` 中的实际端口，不再写死默认地址。
- MySQL、Redis、API、Web 使用 `restart: unless-stopped`，并为 Web 增加容器健康检查。
- Docker Compose 使用交付专用项目名，不会再连接开发环境的容器、网络或数据卷。
- 每次全新安装会生成独立的 Compose 安装标识，多个解压副本也不会误用彼此的数据卷和数据库密码。
- 首次配置现在会在页面内持续显示具体校验错误，并在提交前检查密码强度、账号格式和必填内容。

## 安全边界

项目可以避免自身绑定固定盘符，并能提前识别已知的 Docker 失效磁盘链接问题；但项目不能自动修复接收电脑上未安装 Docker、未启用虚拟化、企业网络阻断镜像下载或 Docker Desktop 自身损坏等外部环境问题。为了保护接收人的既有容器和数据，检查脚本只报告 Docker 全局配置问题，不会自动删除、迁移或覆盖 Docker 数据。
