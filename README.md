# 物业管理平台（PMS3 独立复刻工程）

本工程在不修改上游 Java110 源码的前提下，独立实现物业管理 PC 端的 UI、基础档案、费用配置、收费账务和抄表基础闭环。

当前状态：G0—G10 内部重构与最终验收已完成，当前为 `v0.1.0-internal-rc.1` 发布候选。49 个范围页面全部达到 L1/L2，高置信 IAM、基础档案与费用配置页面达到 L3；不得把内部模拟适配、合成数据或 RC 标签描述为真实生产接入。权威范围、证据和验收标准见：

- `../PMS3目标系统调查/PMS3物业核心复刻工程Goal计划与实施方案.md`
- `docs/PMS3完整重构Goal执行总方案-2026-08-24.md`
- `docs/requirements-traceability.md`

## 技术基线

- Web：Vue 3、TypeScript、Element Plus、Vite；
- API：Java 17、Spring Boot 3.5；
- 数据：加固版 MySQL 8.4、Flyway 11.20、Redis 7.4；
- 本地运行：Docker Compose；
- 外部支付、发票、银行和 IoT：默认模拟适配器；Java110 明确禁用。

## 安全边界

- 不包含目标站凭据、Cookie、令牌或真实住户数据；
- 合成版“优山美地”只保持数量和关系特征；
- 模拟通道不代表生产支付、发票或设备能力；
- 财务成功流水不可物理删除，冲正使用反向流水。

## 已实现范围

- 登录、RBAC、双项目数据范围、企业、组织、岗位、人员、账号和角色权限管理；
- 小区、网格、楼栋、单元、房屋、客户、关系、车位、车辆、仪表等基础档案，以及资产树、客户关系时间线、产权变更和导入预校验；
- 费用定义及会计/税务属性、版本化标准、资产/仪表分配、生效边界、周期/临时应收试算、异步生成、错误明细、幂等重放和配置—账单对账；
- 收银班次、部分/合并缴费、支付模拟、账单/欠费时间切片、预收、押金、折扣审批、票据号段、收据换开/作废、发票换开/红冲、冲正、调账、日结锁定和七项自动对账；
- 仪表主档/资产/父表绑定、幂等抄表批次、跨期连续读数、倍率/损耗/修正快照、异常复核、版本化公摊、IoT 模拟 Inbox、换表凭证、审核后计量费用和三项对账；
- 22 个固定报表读模型、字段白名单、口径/行粒度/固定算例、查询校验和、服务端分页、汇总与钻取；异步 CSV/XLSX/PDF/打印制品、批量收据打印计数和模拟账单通知均持久化留痕；
- 支付、发票、银行信托、IoT 四个模拟器和 Java110 禁用适配器；HMAC 回调、时间窗、Inbox/Outbox、重试、死信/重放、项目隔离、审计、请求追踪、Prometheus 和健康检查；
- TLS/Secret/最小权限部署示例、非 root API/Web/MySQL 镜像、CycloneDX SBOM、依赖/镜像/Secret/SAST 门禁、逻辑备份恢复和独立空库全栈演练；
- 49 页机器可读目录、逐页验收卡、精确路由/权限、查询与列模型、8 类状态定义；
- 按角色/项目隔离的看板配置保存、预览、发布和版本冲突，以及仅保留脱敏字段、模拟门禁来源和敏感导出审计的访客记录工作台；
- 查询、表格、树、批量、金额、状态和任务中心共享原语，浏览器内可运行的 CSV 导入/导出与打印基础；
- 90 张表、22 个 Flyway 迁移、65 个权限、两个隔离的合成项目、五层迁移治理、提交版 OpenAPI 契约与类型漂移门禁；
- 49/49 路由烟雾、三个目标视口 147 张页面基线及 6 张登录/壳层基线，以及 Maven 21 个套件/38 个测试、Vitest 15 个文件/53 个测试、Playwright 21/21 自动化；2026-09-03 四项缺陷修复与回归证据见 `docs/四项缺陷修复与回归报告-2026-09-03.md`。

运行和验收命令见 `docs/runbook.md`，API 见 `docs/api-reference.md`，外部资料差距见 `docs/known-gaps.md`。

G1 登录/壳层与 G2—G10 49 页三个固定视口的像素基线、摘要和受控更新规则见 `docs/visual-baselines.md`；最终 49 页四态矩阵、停止条件和外部门禁见 `docs/acceptance-report.md`，部署与恢复基线见 `docs/deployment-security.md`。

跨设备复制、数据库导出/恢复、新设备启动和完整验收见 `docs/设备迁移与项目交接手册-2026-08-23.md`。
