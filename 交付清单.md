# 物业管理系统交付清单

版本：`v0.1.4-portable-delivery.1`

交付日期：2026-09-03

## 交付物

| 交付物 | 用途 | 是否包含秘密 |
|---|---|---|
| 当前收到的 `.zip` 压缩包 | 可发送的纯净源码分发包 | 否 |
| `物业管理系统交付版使用说明.md` | 首次配置、启动、登录、导入和运维说明 | 否 |
| `检查运行环境.cmd` | 启动前检查 Docker 数据位置、固定盘符、密钥和端口 | 否 |
| `启动物业管理系统.cmd` / `启动物业管理系统.ps1` | 自动启动已安装的 Docker Desktop、生成技术密钥、启动服务并打开网页 | 否 |
| `打开物业管理系统网页.url` | 服务运行后直接打开 `http://localhost:5174` | 否 |
| `物业管理系统-模拟导入-32条合格1条隔离.json` | 可选的迁移中心 32+1 验收样例，不会自动导入 | 否 |
| `物业管理系统-模拟导入数据-32条合格1条隔离.xlsx` | 字段字典、数据明细和验收预期 | 否 |
| `最终交付测试报告-2026-09-03.md` | 首次配置、唯一项目、品牌和构建的最终测试证据 | 否 |
| `DELIVERY-MANIFEST.txt` | 版本、排除项、文件数、大小和校验和 | 否 |

## 纯净包边界

包含运行所需源码、Flyway 迁移、Dockerfile、Compose、锁文件、部署与运维脚本、必要文档和可选导入验收样例。

默认交付不预置业务账号密码。接收人在首次打开网页时自行设置企业、唯一项目和首个管理员；该入口只能成功执行一次。

默认 `PMS_FORMAL_EMPTY_BASELINE=true`。全新数据库完成 V24 后，企业、项目、组织、房屋、客户、车位、车辆、仪表、费用、标准、分配、账单、访客和运行任务均为 0；只保留权限、角色、字典、报表定义、适配器策略及首次配置状态。首次配置才创建当前企业、项目、组织和管理员。

不包含：

- `.git` 与提交历史；
- `.env`、账号密码、Cookie、令牌、JWT/回调密钥；
- 目标系统调查资料和上游参考源码；
- `node_modules`、`target`、`dist`、缓存、日志和扫描证据；
- E2E/单元测试源码、视觉截图基线、测试结果和报告；
- 真实住户、收费、访客或设备数据。

## 可选样例导入预期

| 指标 | 预期 |
|---|---:|
| Raw | 33 |
| Quarantine | 1 |
| Canonical | 32 |
| Staging | 32 |
| Production 实际写入 | 31 |
| PROJECT 映射跳过 | 1 |
| 对账项 | 9，全部 MATCHED |
| 反向变更日志 | 41 |
| 回滚后剩余目标 | 0 |

## 已知边界

- 正式交付指无演示业务数据和可独立部署，不代表外部生产通道已经接通；
- 迁移页面直接接受 JSON，不直接接受 Excel；
- 支付、发票、银行、IoT、访客门禁和 Java110 均为模拟或禁用模式；
- 真实数据迁移、生产密钥、域名/TLS、容量、灾备、UAT 和上线变更窗口需由接收方另行审批和配置。
- Windows 一键启动可以拉起已安装且完成首次初始化的 Docker Desktop，但不会自动安装 Docker、接受 Docker 许可、启用 WSL 2/虚拟化或执行系统重启。
- 便携版不绑定任何固定主机盘符；如果接收电脑的 Docker Desktop 自身仍指向不存在的磁盘，环境检查会在构建前明确拦截并说明修复位置。
