# PMS3 物业核心复刻工程验收报告

验收日期：2026-07-20（本地时区 America/Chicago）

## 结论

工程已达到“可运行、可检查、可继续替换真实业务规则”的物业核心复刻基线。UI/菜单/布局/查询、基础档案、费用配置、应收、收银财务和仪表基础闭环均已落地并连接真实本地 API/MySQL；它不是目标站源码副本，也不应在缺少真实费用、公摊、支付、发票和 IoT 资料时宣称生产等价。

## 交付物验收

| 项目 | 结果 | 主要证据 |
|---|---|---|
| Vue 3 管理端 | 通过 | 登录、主布局、权限菜单、项目切换、通用表格和 3 类专用工作流页 |
| Spring Boot API | 通过 | Java 17 / Boot 2.7，IAM、数据、费用、财务、仪表、适配器分包 |
| MySQL/Flyway | 通过 | 全新数据库 V1—V7 全部成功，50 张表 |
| Redis/本地配置 | 通过 | Redis/MySQL 健康，API 健康 `UP`，Compose 定义通过 |
| RBAC/项目隔离 | 通过 | JWT 含角色、权限、项目；服务入口双重校验 |
| 基础档案 | 通过 | 359 房屋、250 车位、403 客户、车辆/关系/仪表页面与 API |
| 费用/应收 | 通过 | 版本化标准、743 分配、试算和幂等生成实测 |
| 收银财务 | 通过 | 支付订单/确认、收据、模拟开票、预收、押金、冲正实测 |
| 仪表计量 | 有条件通过 | 读数、审核、359 户公摊、换表、计量费用实测；公式待业务确认 |
| 合成数据 | 通过 | 仅复现规模与关系特征，无目标站真实个人数据/ID/金额 |
| 外部适配 | 有条件通过 | 支付、发票、IoT 接口和模拟器完成；真实供应商待接入 |
| 测试与文档 | 通过 | Maven、Vitest、Playwright、OpenAPI、运行/安全/数据/API 文档 |

## 实测数据

### 全新数据库

- Flyway 成功迁移：7；表：50。
- 房屋：359；客户：403；费用分配：743。
- 账单恒等式违规：0；客户资产关系孤儿记录：0。
- 新库启动后健康检查 `UP`，管理员可登录，API 分页查询返回房屋总数 359。

### 费用和财务

- 两项资产试算返回 4 条费用明细；应收生成使用幂等键，重放不产生重复账单。
- 支付订单创建/确认、重复回调、收据、模拟开票、冲正均已执行。
- 预收充值 30、抵扣 5 后余额 25；押金收取 50、退还 20 后余额 30。
- V7 修复两条合成种子四舍五入差异并建立数据库检查约束，当前违规为 0。

### 仪表

- 批次录入 2 条读数；相同读数重放结果为 `created=0, replayed=2`。
- 面积比例公摊覆盖 359 套房屋，请求总量 100，分配合计 100.0000。
- 批次审核后生成 2 条计量费用，总额 71.57；重放结果 `generated=0, replayed=2`。
- 换表后旧表 `REPLACED`、新表 `ACTIVE`，末次/初始读数保存为 88.5000 / 0.0000。

## 自动化结果

| 检查 | 结果 |
|---|---|
| `mvn clean verify` | 10 个测试通过 |
| `npm audit` | 0 漏洞 |
| `npm test` | Dashboard 与专用工作流组件测试 |
| `npm run build` | TypeScript 和生产构建通过；路由/Element Plus 已按需拆包，无 500 KB 警告 |
| `npm run test:e2e` | 2 条真实 API/MySQL 场景通过 |
| 浏览器人工检查 | 1440×900 下应收、收银、仪表页面布局正常；控制台未发现应用错误 |

## 未通过项与处理

本机 Docker Hub 鉴权端点 `auth.docker.io` 网络超时，因此 API/Web 镜像的基础层拉取构建未在本机完成。Compose 配置本身已通过，MySQL/Redis 容器和宿主机 Java/Node 全栈已完成验收。该项恢复外网后按运行手册重试，不涉及业务代码缺失。

## 使用建议

可将本版本作为 UI 和核心业务骨架、字段模型、联调环境和全面代码审查基线。进入真实试运行前，必须补齐已知差距文档中的业务资料，逐项执行历史数据迁移对账、真实通道验签、权限复核、压力测试、备份恢复和安全测试。
