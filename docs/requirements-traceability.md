# 需求追踪矩阵

本文件把正式 Goal 的 14 项交付物映射到代码、测试和验收证据。状态只允许：`未开始`、`实施中`、`已验证`、`外部资料待接入`。

| ID | 交付物 | 计划证据 | 状态 |
|---|---|---|---|
| R01 | Vue 3 + TypeScript + Element Plus 管理端 | `apps/admin-web/package.json`、按需构建、浏览器截图 | 已验证 |
| R02 | Java 17 模块化单体 API | `apps/pms-api/pom.xml`、57 个主源码、Maven 测试；Boot 3.5.16 迁移和证据见 ADR-007 | 已验证 |
| R03 | MySQL Flyway 迁移 | V1—V14、三个独立 MySQL 8 空库路径均迁移 14/14、68 张表、65 个权限 | 已验证 |
| R04 | Redis、数据库和本地启动配置 | Compose 健康检查、API `UP`、运行手册 | 已验证 |
| R05 | 登录、RBAC、项目权限、菜单、布局、查询表格 | IAM、安全上下文、路由守卫、双项目隔离、首次改密、限流审计、会话撤销、65 个权限、有限权限首页和六页完整生命周期 E2E | 已验证 |
| R06 | 基础档案 | 同项目复合外键与停用保护、类型化资产树/档案/客户/关系 API、页面 6/7/20/21 专用工作台、359/250/403/403 有效基线与产权 E2E | 已验证（G3） |
| R07 | 费用定义、标准、分配、应收 | `FeeConfigurationService`、`ReceivableService`/异步处理器、页面 8—11、版本/分配边界、周期/临时试算、幂等生成、不可变快照和三项对账 | 已验证（G5） |
| R08 | 收银、账单、欠费、预收、押金、收据、冲正 | FinanceService、收银页、余额恒等式实测 | 已验证 |
| R09 | 仪表、抄表、公摊、换表、计量费用 | MeterService、专用页面、359 户公摊和计费实测 | 外部资料待接入 |
| R10 | 合成优山美地和扩展数据 | V4 主项目种子、V11 最小隔离项目完整档案链、全新库数量、孤儿/跨项目/重复有效关系/非法面积均为 0 | 已验证（G3） |
| R11 | 支付、发票、IoT、Java110 适配器 | 三类端口和模拟器；Java110 预留禁用 | 外部资料待接入 |
| R12 | 后端/前端/E2E/视觉自动化测试 | Maven 24/24（真实 MySQL 8 项）、Vitest 26/26、Playwright 15/15、49/49 路由烟雾、3 视口 153 张像素基线、产权/迁移/费用应收主旅程 | 实施中（G1—G5 范围已验证，G6—G10 继续扩展） |
| R13 | 完整文档 | README、架构、API、数据字典、运行、安全、49 页验收卡、视觉基线及 G5 阶段验收；随 G6—G10 持续更新 | 实施中 |
| R14 | 最终验收报告 | `docs/acceptance-report.md` 需要在 G10 按新 Goal 重新汇总 | 实施中 |

## 范围页面

| 域 | 页面 | 目标等级 | 状态 |
|---|---|---:|---|
| 平台 | 登录、主布局、工作台、项目切换、权限、字典、审计 | L3 | 实施中（G1 权限/壳层和 G2 页面结构已验证；字典/审计业务闭环继续深化） |
| 企业与 IAM | 企业、组织、角色、岗位、人员、账号与项目权限 | L3 | 已验证（6 个专用路由、完整生命周期和引用保护） |
| 基础档案 | 小区、网格、楼栋、单元、房屋、客户、客户资产关系 | L3 | 已验证（G3：真实 API、树/时间线、产权变更、导入预校验、停用保护） |
| 停车 | 车位、车辆、车辆车位关系 | L3 | 已验证（G3 数据模型与资产档案；后续领域页仍随相关波次深化） |
| 费用配置 | 费用定义、费用标准、费用分配、周期/临时应收预览与异步生成 | L3 | 已验证（G5：版本边界、批量预览、幂等、快照、错误与对账） |
| 收费账务 | 收银台、账单、欠费、预收、押金、收款、收据、冲正 | L2/L3 | 已验证（外部通道模拟） |
| 仪表抄表 | 仪表、批次、读数、公摊、换表、计量费用 | L2/L3 | 已验证（公式待业务确认） |
| 查询、打印与通知 | 22 个查询页、批量打印、账单通知、任务中心 | L2 | 实施中（G2 结构、查询/列模型、导出打印基础与视觉基线已验证；口径和后端任务在 G7） |
| 迁移、访客与外部适配 | 迁移中心、访客记录、银行/支付/发票/IoT 边界 | L2 | 实施中（迁移中心已验证 G4 L2；访客和外部适配继续在 G8/G9） |

## G2 页面原语与验收体系证据

| 退出条件 | 证据 | 结果 |
|---|---|---|
| 49 页都有可追踪骨架 | `src/config/page-catalog.json` 恰好 49 项；8 个菜单组恰好 49 个目标路由；每项含目标路径、波次、类型、查询、列、权限、操作、状态和来源 | 已验证 |
| 49 页都有验收卡 | `docs/page-acceptance-cards.md` 由目录确定性生成；`pages:acceptance:check` 防止手工漂移 | 已验证 |
| 共享能力不是占位弹窗 | `QueryPanel`、`DataGrid`、`TreeWorkspace`、`BatchActionBar`、`TaskDrawer`、`MoneyCell`、`StatusTag`；CSV 导入/导出、打印、取消/重试/部分失败均可运行 | 已验证 |
| 复杂页面没有强塞万能 CRUD | 路由按 specialized / schema / capability、tree / report / external 等实现类型分派；目录测试禁止 `generic-crud` | 已验证 |
| API 契约不漂移 | 提交版 OpenAPI JSON、生成 TypeScript、前端静态检查、运行中检查和 Spring 集成测试五点闭环 | 已验证 |
| 可查看视觉差异 | 49/49 路由烟雾；3 个视口 × 49 页 = 147 张结构基线，首次生成后纯比对通过；另有 G1 6 张登录/壳层基线 | 已验证 |

G2 的“已验证”仅对应总方案第 7 节的页面原语与验收体系退出条件。目录中仍使用结构演练数据的页面会明确标识合成/脱敏和后续 Goal 波次；不得据此把 49 页标记为 L2/L3 全部完成。

## G3 基础档案纵向闭环证据

| 退出条件 | 证据 | 结果 |
|---|---|---|
| 层级主数据和统一资产模型 | Flyway V11；`grid_area`、`building`、`pms_unit`、`asset` 复合外键；房屋/车位类型明细原子创建 | 已验证 |
| 客户、权属、租住与历史 | 类型化资产/客户档案、关系起止、产权转移、`property_relation_event` 不可变时间线和幂等重放 | 已验证 |
| 项目隔离与档案停用 | 双项目 API 集成测试、跨项目引用 403/400、被引用档案 409、软停用与乐观锁 | 已验证 |
| 导入模板和校验报告 | 6 类 CSV 模板；最多 500 行的无写入逐行校验；引用、重复、面积、日期和重叠错误可下载/展示 | 已验证 |
| 数据质量可重复 | 主项目 359/250/403/403；隔离链 1/1/1/2/1；孤儿、跨项目、重复有效关系、非法面积均为 0 | 已验证 |
| 房产—客户主旅程 | Playwright 创建客户→产权转移→幂等重放→双工作台/时间线→恢复全部原产权关系→停用客户；连续两次 2/2 且计数不变 | 已验证 |
| 页面与视觉 | 房产、客户、网格、车位 4 页接入真实域；IAM 默认有效态与组织树筛选消除软停用历史污染；G3 共更新 36 张受审图片；147/147 纯比对、5 worker 并发全量和人工抽检通过 | 已验证 |

G3 完成不代表整个 Goal 完成。G4 已在下节完成合成/脱敏首轮迁移演练；经授权的正式历史数据、对象存储和大文件断点续跑仍属于外部门禁。

## G4 数据迁移中心证据

| 退出条件 | 证据 | 结果 |
|---|---|---|
| 五层边界与批次治理 | Flyway V12 的 9 张迁移表；Raw、Quarantine、Canonical、Staging、Production 计数、SHA-256、映射版本、审核人、回滚点和事件 | 已验证 |
| 同批安全重放 | `(community_id, source_sha256, mapping_version)` 唯一；创建、执行和回滚重复请求返回原批次/`replayed=true`，对象数量不增加 | 已验证 |
| 错误隔离与部分失败 | 32 条合格 + 1 条错误样本；错误只进入 Quarantine，Canonical/Staging 为 32，审批必须显式确认部分执行 | 已验证 |
| 受控生产写入 | PROJECT 映射 1 条；BUILDING/ROOM/CUSTOMER/RELATION 写入 31 条；41 条含房屋明细的反向变更记录；仅白名单资源和参数化 SQL | 已验证 |
| 数量、关系和面积对账 | 5 类对象数量、建筑面积、可用面积、孤儿关系、重复有效关系共 9 项全部 `MATCHED` | 已验证 |
| 可逆回滚 | 错误凭证拒绝；正确凭证按 41 条变更逆序删除，剩余目标数 0，Raw/错误/映射/事件/审计证据保留 | 已验证 |
| 页面、契约和浏览器主旅程 | 页面 17 专用 `MigrationCenterView`、类型化 API/OpenAPI、Vitest、Playwright 主旅程、三个视口受控基线 | 已验证 |

G4 完成只代表受控合成样本的迁移管线通过。未取得经授权的目标逐行数据、正式映射签字和财务期初账时，不得把本结果描述为生产历史迁移完成。

## G5 费用配置与应收证据

| 退出条件 | 证据 | 结果 |
|---|---|---|
| 定义、财税属性和标准版本 | V13 定义精度/舍入/税务/币种约束；创建/更新定义、创建标准、追加版本、停用标准 API；完整配置事件 | 已验证 |
| 版本和分配生效边界 | 8 月版本/9 月版本边界集成测试；ACTIVE 区间重叠拒绝；资产/仪表分配预览、批量写入、请求重放和按日期取消 | 已验证 |
| 公式、精度和舍入 | `BUILDING_AREA`、`USABLE_AREA`、`FIXED`、`METER_USAGE`；系数、最小/最大金额、定义级 0—2 位和四种舍入；HALF_EVEN 精确样例 | 已验证 |
| 周期与临时应收 | 页面 10 周期试算/任务/错误/对账，页面 11 独立临时明细/试算/任务；临时定义必须显式允许 | 已验证 |
| 幂等与历史不漂移 | 请求键 + 请求哈希；同键同请求返回原任务，同键不同请求 409；周期账单唯一键；配置更新后历史账单快照不变 | 已验证 |
| 异步、错误和对账 | 受控任务执行器、逐行 GENERATED/SKIPPED/FAILED、错误明细；完成任务三项对账全部 MATCHED、运行库差异为 0 | 已验证 |
| 空库、契约和全栈页面 | V1—V14/68 表；提交版/生成类型/运行中 OpenAPI 一致；Maven 24/24、Vitest 26/26、Playwright 15/15、页面 8—11 三视口基线 | 已验证 |

G5 的“已验证”使用确定性合成费用、税率、科目、资产、临时费用和账单。业务方尚未批准真实收费规则和财务期初数据，因此不得描述为生产计费口径已确认；下一检查点为 G6 收银与不可变账务。

## 合成数据验收基线

| 对象 | 基础种子数量 |
|---|---:|
| 项目 | 2（1 个主项目 + 1 个合成隔离项目） |
| 网格 / 楼栋 / 单元 | 1 / 9 / 5（含隔离项目最小链） |
| 房屋 | 360（主项目 359） |
| 车位 | 251（主项目 250） |
| 客户 | 404（主项目 403） |
| 客户资产有效关系 | 404（主项目 403） |
| 仪表基础样本 | 32（主项目 31） |
| 费用定义 | 23（含 1 个临时费用合成示范） |
| 计费标准 | 16 |
| 房屋费用分配 | 549 |
| 车位费用分配 | 194 |

所有 ID、姓名、电话、房号和金额均为程序生成，不得来自目标站原始记录。
