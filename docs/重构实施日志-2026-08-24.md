# PMS3 重构实施日志

日期：2026-08-24

## 本批目标

按《PMS3 重构前完整扫描与实施方案》启动阶段 0，并提前落地阶段 1 的应用壳层样板。当前工程仍定位为独立重构环境；目标站凭据、个人信息和业务秘密不得写入源码、测试夹具或 Git。

## 已完成

1. 建立本地 Git 基线，基线提交为 `1fc4f7f`；`.env`、运行数据、构建产物和测试截图均被忽略。
2. 增加统一 REST 认证入口，缺失、无效或过期 JWT 均返回带请求 ID 的 401 JSON。
3. 消除 E2E 对历史抄表批次的依赖，改为每次生成唯一批次号。
4. 修复 npm 传递依赖，完整 `npm audit` 为 0 漏洞。
5. 重构登录页与应用壳层：
   - 目标式白色登录区和蓝色产品横幅；
   - 42px 蓝色顶栏、160px 深色侧栏；
   - 按目标信息架构重新分组菜单；
   - 最多 12 个会话级工作页签，支持切换、关闭、关闭其他和刷新；
   - 项目选择、权限过滤和退出登录保持可用。
6. 增加普通员工双项目范围、越权项目、平台管理员和显式权限单元测试。
7. 增加 JaCoCo 覆盖率报告与真实 MySQL Flyway/Testcontainers 迁移测试。
8. 将 Testcontainers 从 1.19.8 升至 1.21.4，以兼容当前 Docker Engine API。

## 验证结果

| 检查 | 结果 |
|---|---|
| 前端类型检查与生产构建 | 通过 |
| Vitest | 2 个文件、3 个测试通过 |
| Playwright E2E | 2/2 通过 |
| npm audit | 0 漏洞 |
| 后端常规验证 | 14 个测试，13 通过、1 个容器测试在无 Docker socket 的镜像构建内跳过 |
| MySQL Testcontainers 独立执行 | 1/1 通过，7 个 Flyway 迁移及 359 套房产、250 个车位基线验证通过 |
| 认证失败语义 | 无令牌 401、无效令牌 401 |
| 运行状态 | API、Web、MySQL、Redis 均已启动；有健康检查的服务均为 healthy |
| 1440×900 视觉检查 | 无页面级横向溢出，顶栏、侧栏、页签和房产列表显示正常 |

## 当前边界

- 当前业务库仍以脱敏合成数据为主；本批没有把目标生产数据直接导入开发库。
- 已取得的目标站只读调查结果只用于字段、数量、页面和口径分析。
- 真实数据进入重构系统前，仍必须经过迁移批次、字段映射、脱敏、校验、对账和可回滚流程。
- 当前只有本地 Git 仓库，尚未配置私有远程仓库。

## 下一批

1. 完成组织、人员、角色、权限和项目范围的管理闭环及 API 级越权测试。
2. 建立第二个合成项目及普通员工可登录测试账号，用于跨项目隔离 E2E。
3. 建立 49 页验收卡、指标口径模板与视觉基线快照。
4. 设计并落库 `migration_batch`、源目标映射、错误明细和对账结果表。
5. 以房产—客户关系为首条纵向链路，选择 20–50 个脱敏资产做迁移演练。

## G1 批次 1：企业组织与项目权限核心闭环

执行日期：2026-08-24

### 本批完成

1. 新增 Flyway V8，建立企业、组织单元、岗位、人员模型，并为项目补齐企业归属；运行库从 V1—V8 迁移成功，共 54 张业务表。
2. 增加第二个合成隔离项目“海湾雅居（合成隔离项目）”，原“优山美地（合成示范项目）”数据规模保持 359 套房产、250 个车位不变。
3. 新增企业、组织、岗位、人员、角色、权限、账号、项目范围的显式 DTO、领域服务和 `/api/v1/iam` 管理接口；核心写操作包含验证、乐观锁、引用约束和审计。
4. 增加 `iam:read`、`iam:write` 权限，并将有效角色、有效项目范围纳入登录身份加载；登录结果暴露首次改密标识并记录最近登录时间。
5. 管理端新增企业管理、组织管理、角色管理、岗位管理、人员管理、账号与项目权限 6 个专用路由和工作台；支持组织树、角色权限、账号角色/项目范围、状态维护和密码重置。
6. 菜单和路由按 `iam:read` 过滤，API 仍执行服务端权限与项目范围校验，前端隐藏不作为安全控制。
7. 新增真实 MySQL 项目隔离集成测试：普通项目管理员只能读取授权项目，访问第二项目返回 `403 PROJECT_ACCESS_DENIED`，访问 IAM 管理 API 返回 403。
8. 扩展 Playwright 场景：管理员创建普通账号、普通账号登录、项目选择仅显示授权项目、跨项目请求 403、IAM 路由跳转无权页；临时账号在 `finally` 中自动停用。
9. 用正在运行的 OpenAPI 重新生成前端类型；修复审计参数对空项目/资源值的安全绑定，企业级 IAM 审计不再伪造项目外键。

### 本批验证证据

| 检查 | 结果 |
|---|---|
| Flyway 运行库 | V8 成功；2 个有效项目、3 个组织单元、2 个 IAM 权限 |
| 后端 Docker 无缓存 `mvn -B verify` | 15 个测试，0 失败、0 错误；镜像内 2 个 Testcontainers 用例因无 Docker socket 跳过 |
| 宿主机真实 MySQL/Testcontainers | V1—V8 空库迁移和 IAM 双项目隔离两项分别通过 |
| 前端 Vitest | 2 个文件、3 个测试通过 |
| 前端类型与生产构建 | 通过 |
| Playwright E2E | 3/3 通过 |
| npm audit | 0 漏洞 |
| OpenAPI 类型生成 | 通过，`src/api/generated.ts` 已更新 |
| 浏览器真实检查 | 登录、管理员双项目、6 个 IAM 入口、账号角色/项目范围表格均可见 |
| 测试数据卫生 | 启用的 `e2e-project-user-*` 为 0；本轮遗留账号已通过 IAM API 停用 |
| 运行状态 | API healthy；Web、MySQL、Redis 运行中 |

### 阶段判断

G1 的核心数据模型、管理页面和跨项目默认拒绝链路已经形成，但 **G1 尚未完成**。仍需完成以下内部项后才能通过 G1 退出门槛：

- 企业、组织、岗位、人员、角色、账号的完整生命周期 E2E（正常、校验、冲突、停用和引用失败）；
- 登录限流、失败审计、会话撤销/令牌失效和首次改密强制流程；
- 按钮级 `iam:write` 控制及权限变更后旧会话的即时失效验证；
- 1366×768、1440×900、1920×1080 三个视口的固定视觉基线；
- Spring Boot 支持版本升级 ADR。

下一工作包继续完成上述 G1 收尾；G1 完成后进入 G2 的 49 页验收卡、页面原语和视觉差异体系。G1 完成不代表 G2—G10 完成。

## G1 批次 2：登录安全、会话撤销与权限写控制

执行日期：2026-08-24

### 本批完成

1. 新增 Flyway V9，为账号增加 `session_version`、`password_changed_at`，并建立 `auth_login_guard`；运行库升级到 V9，共 55 张表。
2. 登录失败按账号和来源 IP 的 SHA-256 摘要计数，支持可配置时间窗、失败阈值和锁定时间；登录成功、失败和限流均写 `audit_event`，测试确认不保存原始账号或 IP。
3. JWT 增加会话版本声明，每个 Bearer 请求都会核对账号启用状态和数据库会话版本。账号、密码、角色权限变化及主动撤销会话都会让旧令牌即时失效。
4. 增加强密码策略：至少 12 位，必须含大小写字母、数字和符号，不能包含完整账号名或复用当前密码；IAM 创建和重置密码统一执行该策略。
5. 新账号固定标记首次改密；服务端在改密前仅允许 `/auth/me`、`/auth/change-password` 和 `/auth/sessions:revoke`，防止绕过前端访问业务 API。改密成功返回新令牌。
6. 管理端增加独立首次改密页和路由强制跳转，登录后根据服务端标识进入改密流程。
7. 页面按钮按 `iam:write` 控制，同时保留服务端 `iam:write` 强制校验。只读角色可查看 IAM，不能看到新增、编辑和重置密码操作，直接写 API 返回 `403 PERMISSION_DENIED`。
8. OpenAPI 类型已按运行中 V9 API 重新生成；Compose 暴露三项登录保护参数，示例环境文件和运行/安全/API 文档已同步。
9. 形成 ADR-007：G1 退出前以独立提交把 Spring Boot 2.7.18 迁移到受支持的 3.5.x，不把框架升级与业务迁移混在同一提交。

### 本批验证证据

| 检查 | 结果 |
|---|---|
| Flyway 运行库 | V9 成功、55 张表；`sys_user` 两个 V9 字段存在 |
| 宿主机真实 MySQL/Testcontainers | V1—V9 空库迁移 1/1；IAM/安全集成 2/2，通过首次改密、旧会话失效、限流与脱敏审计 |
| 后端 Docker `mvn -B verify` | 18 个测试，0 失败、0 错误；镜像内 2 个 Testcontainers 类因无 Docker socket 跳过 |
| 前端 Vitest | 2 个文件、3 个测试通过 |
| 前端类型与生产构建 | OpenAPI 生成、TypeScript 和 Vite 构建通过 |
| Playwright E2E | 4/4 通过；含首次改密、跨项目拒绝和 IAM 只读/写权限 |
| npm audit | 0 漏洞 |
| 测试数据卫生 | 启用的 `e2e-*` 账号和 `E2E_*` 角色均为 0 |
| 运行状态 | API、Web 镜像重新构建；API、MySQL、Redis healthy，Web 运行中 |

### 阶段判断

登录限流/审计、首次改密、会话撤销、权限变更旧令牌失效和按钮级写权限已经完成，但 **G1 仍未通过退出门槛**。剩余内部项为：

- 企业、组织、岗位、人员、角色、账号的完整生命周期 E2E，包括校验、唯一冲突、乐观锁、停用和引用失败；
- 1366×768、1440×900、1920×1080 三个视口的固定视觉基线；
- 按 ADR-007 完成 Spring Boot 3.5.x 实际迁移并重跑全套门禁。

完成这些项目后才进入 G2；即使 G1 通过，G2—G10 和整个 Goal 仍然未完成。

## G1 批次 3：Spring Boot 3.5 支持版本迁移

执行日期：2026-08-24

### 本批完成

1. 按 ADR-007 把 API 从 Spring Boot 2.7.18 独立迁移到 3.5.16，保持 Java 17，不混入业务表或领域行为变更。
2. 将 Servlet 与 Validation 从 `javax.*` 全量迁移到 `jakarta.*`，源码扫描不再存在 `import javax.`。
3. 将 Spring Security 5 配置迁移到 Security 6 的 `SecurityFilterChain`、`authorizeHttpRequests`、`requestMatchers` 和方法级安全配置；继续保持无状态 JWT、401/403 语义和安全响应头。
4. 将 springdoc 1.7 迁移到 springdoc 2.9 starter，并用运行中 OpenAPI 重新生成 TypeScript 类型。
5. 将 Redis 配置迁移到 Boot 3 属性命名；Flyway 11、MySQL 驱动、Testcontainers 和 V1—V9 迁移保持兼容。
6. 排除 Spring 默认内存用户自动配置，启动日志不再生成临时密码，系统只使用已有 JWT/IAM 认证链路。

### 本批验证证据

| 检查 | 结果 |
|---|---|
| 宿主机 Maven `verify` | 19 个测试，0 失败、0 错误、0 跳过；含真实 MySQL V1—V9 空库迁移和 IAM/安全集成测试 |
| 后端 Docker `mvn -B verify` | 19 个测试，0 失败、0 错误；镜像内 3 项 Testcontainers 测试因无 Docker socket 按预期跳过 |
| Docker 运行状态 | API 使用 Boot 3.5.16 启动并 healthy；MySQL/Redis healthy；运行库 9 个迁移验证通过 |
| OpenAPI | `/v3/api-docs` 返回 200，生成文档约 35 KB，TypeScript 类型生成成功 |
| 前端 Vitest | 2 个文件、3 个测试通过 |
| 前端类型与生产构建 | 通过 |
| Playwright E2E | 4/4 通过，原 IAM、首次改密和项目隔离语义无回退 |
| npm audit | 0 漏洞 |

### 阶段判断

ADR-007 的框架升级检查点已经完成，但 **G1 仍未通过退出门槛**。剩余内部项不变：

- 企业、组织、岗位、人员、角色、账号的完整成功和失败生命周期自动化；
- 1366×768、1440×900、1920×1080 三个视口的固定视觉基线。

上述两项完成并复跑 G1 全部门禁后才能进入 G2；G2—G10 仍需继续执行。

## G1 批次 4：IAM 完整生命周期与引用边界

执行日期：2026-08-24

### 本批完成

1. 增加真实 MySQL 生命周期集成场景，覆盖企业、组织树、岗位、人员、角色和账号的创建、查询、更新、停用及完整关系链。
2. 覆盖 Bean Validation、唯一冲突、跨企业项目/组织/角色、组织循环、人员离职日期、空角色、乐观锁、密码重置、账号停用、旧会话失效、自停用保护和平台管理员保护。
3. 修复跨企业授权边界：启用账号关联人员后，企业角色和项目范围必须与人员属于同一企业；启用账号不能分配停用人员、角色或项目。
4. 增加 `IAM_RESOURCE_IN_USE` 稳定冲突语义：企业、组织、岗位、人员和角色仍有启用引用时拒绝停用，要求调用方按依赖顺序处理。
5. Playwright 新增六页贯通场景：创建整条 IAM 关系链，在企业、组织、岗位、人员、角色和账号页逐一确认，再验证错误分支、重置密码和依赖顺序停用，最后确认六页状态。
6. E2E 清理使用当前版本重新读取并按账号→人员→岗位→组织→角色→企业顺序执行；运行库核对所有 `e2e-*`/`E2E_*` 启用对象均为 0。

### 本批验证证据

| 检查 | 结果 |
|---|---|
| 宿主机 Maven `verify` | 20 个测试，0 失败、0 错误、0 跳过；真实 MySQL IAM 3/3、V1—V9 空库迁移 1/1 |
| 后端 Docker `mvn -B verify` | 20 个测试，0 失败、0 错误；镜像内 4 项 Testcontainers 测试因无 Docker socket 按预期跳过 |
| Playwright E2E | 5/5 通过；新增六页完整生命周期、冲突与引用保护 |
| 前端 Vitest | 2 个文件、3 个测试通过 |
| 前端类型与生产构建 | 通过 |
| npm audit | 0 漏洞 |
| 测试数据卫生 | 启用的 E2E 账号、角色、企业、组织、岗位、人员均为 0 |
| 运行状态 | API/健康检查 `UP`；API、MySQL、Redis healthy，Web 运行中 |

### 阶段判断

企业、组织、岗位、人员、角色和账号的 CRUD/生命周期退出条件已经完成，G1 当前只剩：

- 1366×768、1440×900、1920×1080 三个视口的固定视觉基线及证据清单。

视觉基线和 G1 全门禁复跑通过后才可把 G1 标记完成并进入 G2；整个 Goal 仍有 G2—G10。

## G1 批次 5：三视口视觉基线与 G1 退出

执行日期：2026-08-25

### 本批完成

1. 新增 `visual-baselines.spec.ts`，固定覆盖 1366×768、1440×900、1920×1080 三个目标视口。
2. 每个视口同时保存空白登录页和登录后项目看板，共 6 张受版本控制的 Windows/Chrome 像素基线。
3. 自动校验页面无横向溢出，以及顶栏 42px、侧栏 160px、页签 38px、主区域宽度和起点；截图关闭动画、隐藏光标并设置 0.5% 抗锯齿容差。
4. 对 6 张基线逐张人工检查；登录卡片、导航、顶栏、侧栏、页签、指标卡和账务/质量区域在三个尺寸下均未发现裁切、重叠或页面级横向滚动。
5. 新增 `docs/visual-baselines.md`，记录文件、尺寸、字节、SHA-256、运行方式、受控更新规则和平台边界。
6. 使用内置浏览器在三个目标视口复核登录页 DOM：`body` 和根节点的 `scrollWidth` 均等于 `clientWidth`，检查后已恢复默认视口。

### 本批验证证据

| 检查 | 结果 |
|---|---|
| Playwright 全量 | 8/8 通过；5 个功能场景 + 3 个视口视觉场景 |
| 像素基线 | 6/6 首次生成后独立复跑对比通过 |
| 视口几何 | 3/3 无页面级横向溢出；42/160/38px 壳层尺寸断言通过 |
| 人工视觉检查 | 6/6 无裁切、重叠或异常横向滚动 |

### 阶段判断

G1 的企业、组织树、岗位、人员、账号、角色、权限矩阵、第二合成项目、项目/按钮/数据权限、登录保护、会话撤销、完整生命周期和三视口视觉基线均已有自动化及文档证据。完成本批最终构建、安全、数据卫生和干净工作树检查后，G1 可通过退出门槛并进入 G2。

G1 完成只代表第一阶段完成；G2—G10 和完整 Goal 仍然未完成。

## G2：页面原语与验收体系

执行日期：2026-08-25

### 批次 1：49 页机器可读目录与验收卡

1. 建立 `src/config/page-catalog.json`，严格固定 49 个目标页面及 1—49 连续编号，逐页记录波次、领域、新路由、目标路径、标题、实现类型、目标等级、查询字段、列模型、读写/导入/导出/打印权限、操作、来源证据和待确认假设。
2. 固定 normal、loading、empty、forbidden、validation-error、request-error、conflict、partial-failure 八类状态，并用类型化适配层暴露给路由和页面。
3. 新增验收卡生成器，确定性生成 `docs/page-acceptance-cards.md`；`pages:acceptance:check` 会阻止目录与文档漂移。
4. 单元测试强制检查恰好 49 页、编号/路径/标题/来源唯一、查询列权限完整、八类状态齐全，并禁止把复杂页面标记为 `generic-crud`。

检查点提交：`945546e`。

### 批次 2：共享页面原语、任务中心与 49 页路由

1. 新增 `QueryPanel`、`DataGrid`、`TreeWorkspace`、`BatchActionBar`、`TaskDrawer`、`MoneyCell`、`StatusTag`，并将现有通用数据页迁移到共享原语。
2. 任务中心实现 QUEUED/RUNNING/SUCCEEDED/PARTIAL_FAILED/FAILED/CANCELLED 状态、取消、重试、下载和会话元数据恢复；CSV 导出防公式注入，CSV 导入校验大小/表头/行并可下载部分失败明细，打印生成可下载 HTML。
3. 顶层菜单按目标系统信息架构固定为 8 组、49 个目标页面；支持性工作流仍可直接路由，但不计入 49 页。
4. 专用页、已有 schema 页和 capability 工作区按元数据分派，不使用单一万能 CRUD。结构演练数据和外部模拟适配器在界面中明确标识，不冒充目标生产数据或真实通道。
5. Flyway V10 增加 53 个页面/按钮权限，总权限数达到 65；平台管理员获得 65 项，项目经理、收银员、抄表员只获得各自范围。

检查点提交：`388f4b9`、`eb6499c`。

### 批次 3：OpenAPI 漂移链、烟雾与视觉差异体系

1. 提交后端 OpenAPI JSON 快照和前端生成 TypeScript；静态 `api:check`、运行中 `api:check:live` 和 Spring 集成测试共同阻止契约未同步变更。
2. 新增 49 页路由烟雾测试，逐页验证最终 URL、标题、壳层、非“开发中”内容和 body/root 无横向溢出。
3. 烟雾测试发现收银台打开 12 个页签后操作区越出 1280 宽视口；修复页签 `min-width`/伸缩规则及固定操作区后，49/49 通过。
4. 三个目标视口逐页建立 147 张 Windows/Chrome 结构基线，首次写入后进行无更新纯比对，3/3 测试、147/147 图片通过。
5. 人工抽检看板、树形组织、收银台、查询报表、银行模拟适配和访客页，未发现空白、裁切、重叠或异常横向滚动。
6. 全量 E2E 首轮发现 IAM 只读账号首次改密后被固定送往无权限看板；新增首个授权工作区选择器，统一登录、改密、403 返回和页签首页行为，独立复跑及全量复跑均通过。

检查点提交：`fc21565`、`7ee433f`、`4b20e56`。

### G2 最终门禁证据

| 检查 | 结果 |
|---|---|
| 前端单元/组件/目录/契约 | Vitest 7 个文件、20/20；49 页验收卡一致；提交版 OpenAPI 类型一致 |
| 前端生产构建 | TypeScript、Vue 类型检查和 Vite 构建通过；1647 个模块转换成功 |
| 依赖审计 | `npm audit --audit-level=high` 为 0 漏洞 |
| Playwright 全量 | 12/12；核心档案、应收/收银/抄表、普通员工项目隔离、IAM 只读、六页 IAM 生命周期、49 页烟雾、G1 视觉和 G2 视觉全部通过 |
| 49 页路由 | 49/49 可达，标题/内容/权限元数据可追踪，无“开发中”，无页面级横向溢出 |
| 视觉 | G2 147/147 首次生成后纯比对通过；G1 6/6 通过；代表页面人工抽检通过 |
| 后端宿主机 `clean verify` | 21/21，0 失败、0 错误、0 跳过；真实 MySQL 集成 5/5 |
| Flyway 空库 | 两个独立 MySQL 8 测试库均从空库完成 V1—V10；运行库处于 V10 |
| OpenAPI 运行中检查 | `/v3/api-docs` 与提交 JSON、生成 TypeScript 一致；API 健康 `UP` |
| Docker | API/Web 以当前源码重建；API、Web、MySQL、Redis 全部运行，API healthy，Web `/login` 返回 200 |
| 测试数据卫生 | 启用 E2E 账号/角色、ACTIVE E2E 企业/组织/岗位/人员均为 0；历史人员以 `LEFT` 软停用保留审计链 |
| 秘密卫生 | Git 未追踪 `.env`；追踪文件中未发现私钥或长 Bearer/API 令牌模式 |

49 页视觉集合摘要：1366×768 为 `d12028ef993470be3763eb99947c1f7dd06e938225067c80314902dd4435df98`，1440×900 为 `d3530f3786a55652e7b291232b4a3b052c6f88b0c199237b988e6de9918962c5`，1920×1080 为 `8aca8c603452f94f19060aa8c9751e6015df17dad08c53deb37c9aa347ed2419`。计算口径见 `docs/visual-baselines.md`。

### G2 阶段判断

G2 的三个退出条件已经满足：49 页均有可追踪骨架和验收卡；导入、导出、打印和任务中心不是占位弹窗；复杂领域页按专用/schema/capability 类型分派。G2 可以通过阶段退出并进入 G3。

这只表示页面原语和验收体系完成。结构演练页仍需在 G3—G9 接入真实领域 API、状态机、迁移、报表口径和服务端任务；G3—G10 及整个 Goal 仍未完成。

## G3：基础档案纵向闭环

执行日期：2026-08-25

### 批次 1：数据库边界与双项目完整档案链

1. Flyway V11 为网格增加父级、排序和版本，为楼栋/资产增加网格引用，为单元和客户资产关系补齐 `community_id`，并在添加约束前修复旧合成数据中楼栋—单元不一致与可用面积大于建筑面积的问题。
2. 为网格、楼栋、单元、资产、客户、车辆、仪表、客户资产关系和车辆车位关系增加项目内复合唯一键/外键、状态/面积/日期检查约束；用生成列唯一键禁止重复有效关系。
3. 新增 `property_relation_event`，保存关系开始、结束、产权转移的请求键、操作者、原因和前后关系快照；关系业务行和事件均不物理删除。
4. 为第二合成项目补齐 1 网格、1 楼栋、1 单元、1 房屋、1 车位、1 客户、1 客户资产关系、1 车辆、1 车辆车位关系和 1 仪表的最小完整隔离链；主项目 359/250/403 规模保持不变。
5. 真实 MySQL 空库迁移连续发现并修复旧资产层级不一致、非法面积、客户编号截断碰撞和自引用仪表复合外键建表顺序；最终两个独立 MySQL 8 测试库均从空库完成 V1—V11。

检查点提交：`07028ce`。

### 批次 2：类型化属性域 API、生命周期与契约

1. 新增资产树、资产列表/档案、客户列表/档案、关系开始/结束、产权转移、六类 CSV 模板和逐行预校验 API；手机号、证件只返回脱敏字段。
2. 关系写操作以 `Idempotency-Key` 重放，资产/关系使用版本号防止静默覆盖；同项目有效引用、关系重叠、档案状态和停用依赖在服务端再次校验。
3. 通用数据层新增真实网格和单元资源、跨项目引用校验、网格循环保护及各级停用保护；创建房屋/车位时原子创建类型明细，停用资产同步设置 `enabled=false` 与 `operation_status=INACTIVE`。
4. 修正仓库根 `.gitignore` 中无意忽略所有 Java `common/data` 包的 `data/` 规则为仅忽略根目录 `/data/`，确保全新克隆包含通用数据层源码。
5. 提交并同步 OpenAPI JSON、生成 TypeScript 和前后端漂移门禁；运行中 `/v3/api-docs` 与两份提交物一致。

检查点提交：`963a496`、`7c2e45b`。

### 批次 3：房产/客户工作台、导入预校验与主旅程

1. 页面 6 房产信息和页面 21 车位信息改为统一资产工作台：真实资产树、分页列表、档案抽屉、客户关系、车辆、仪表、历史、关系起止、产权转移、软停用和 CSV 预校验。
2. 页面 7 客户信息改为客户关系工作台：默认有效客户、真实汇总、档案/车辆/资产关系/时间线、关系登记/结束、停用和 CSV 预校验；页面 20 网格管理改为真实可写资源。
3. 新增房产—客户 Playwright 主旅程：创建临时客户、读取当前产权人、产权转移与幂等重放、双工作台和时间线验收、恢复全部原 OWNER/CO_OWNER、软停用临时客户。
4. 全量后端首轮发现权限隔离测试把资产总数写死为 609，会受到同类测试保留的已停用历史行影响；断言改为明确查询 `status=NORMAL`，继续严格验证 609 条有效资产，而不是放宽业务规则。
5. 数据对账发现产权转移会按设计结束共有产权人，但早期 E2E 清理只恢复主产权人；测试现会快照并恢复全部产权关系。本地演示库通过正式 API 恢复缺失的合成共有产权关系，连续运行两次后有效关系仍稳定为 403。
6. 最终并发套件发现 IAM 软停用历史会使视觉页记录数随 E2E 次数增长；IAM 页面现默认有效态、组织树同步筛选，测试按需切换到停用/离职状态验收历史，截图前移开鼠标消除权限文本悬浮提示。修复后 5 worker 并发全量 13/13。

检查点提交：`5292232`、`a632dd9`；主旅程清理修复与本批文档随 G3 收口提交。

### G3 最终门禁证据

| 检查 | 结果 |
|---|---|
| 前端单元/组件/目录/契约 | Vitest 8 个文件、22/22；49 页验收卡一致；提交版及运行中 OpenAPI 一致 |
| 前端类型/生产构建 | Vue/TypeScript 类型检查通过；Vite 1663 个模块转换成功 |
| 依赖审计 | `npm audit --audit-level=high` 为 0 漏洞 |
| Playwright 全量 | 13/13；含 49 页烟雾、153 张基线、权限/IAM、核心业务和房产—客户产权生命周期 |
| 主旅程可重复性 | 房产—客户 E2E 串行连续执行 2/2；前后有效关系 403，原 OWNER/CO_OWNER 集合一致 |
| 视觉 | 12 张档案领域图片 + 15 张 IAM 稳定图片 + 9 张精确重录字节级图片，共 36/147 受审变化；147/147 无更新纯比对，9 个关键页面人工抽检通过 |
| 后端宿主机 `clean verify` | 22/22，0 失败、0 错误、0 跳过；真实 MySQL 集成 6/6 |
| Flyway 空库 | 两个独立 MySQL 8 测试库均从空库完成 V1—V11；56 张表、65 个权限 |
| 运行库对账 | 主项目有效房屋/车位/客户/关系为 359/250/403/403；隔离链 1/1/1/2/1；孤儿、跨项目、重复有效关系、非法面积均为 0 |
| Docker | API、MySQL、Redis healthy，Web 200；运行库 Flyway V11 |
| 秘密卫生 | `.env` 未被 Git 追踪；追踪文件高置信私钥、长 Bearer、AWS 访问键模式 0 |

49 页视觉集合摘要：1366×768 为 `872af83adf25e1c1b99179057c664176369dfd25d6ea35c93d04b478e885de89`，1440×900 为 `b75c593758ab5b435f1e63b0cf04f926860bf949ce8dc59e8dfdcb9e4c9b6863`，1920×1080 为 `43f3bf2506a3d2bde0b60863b8a73c303d7257f578368be52d68176cc67745f6`。计算口径见 `docs/visual-baselines.md`。

### G3 阶段判断

G3 的退出条件已满足：档案数量可重复对账，孤儿/跨项目/重复有效关系为 0，房产—客户主旅程可重复且清理完整，房产/客户/网格/车位页面已由结构演练切换到真实领域能力。G3 可以通过阶段退出并进入 G4 迁移中心。

这只表示基础档案纵向闭环完成。实际批量迁移落库、断点续跑、对账回滚、费用/账务深化、22 张查询报表、打印通知、审计任务中心与外部适配仍在 G4—G10；完整 Goal 仍未完成。

## G4：数据迁移中心与首轮演练

执行日期：2026-08-25

### 批次 1：V12 五层治理模型

1. 新增 Flyway V12 与 9 张迁移治理表：批次、Raw、Quarantine、Canonical、Staging、对象映射、对账、生产变更日志和批次事件。
2. 批次记录项目、来源名称/类型、服务端 SHA-256、映射版本、行数、错误/导入/跳过计数、申请人、审核人、回滚凭证、乐观版本和完整时间戳。
3. `(community_id, source_sha256, mapping_version)` 阻止同一来源重复建批；Raw 保留逐行 JSON 与 SHA-256，服务层不修改/删除 Raw。
4. 生产写入只允许 PROJECT、BUILDING、ROOM ASSET、CUSTOMER、RELATION 白名单资源；来源 ID 只进入映射表，不充当公开目标主键。
5. 每个生产新增对象和 `room_detail` 都记录执行顺序与快照；回滚只使用目标表白名单，并按执行顺序倒序处理。

### 批次 2：类型化 API 与状态机

1. 新增批次列表/详情、创建、校验、审批、执行、对账、回滚和模板 9 类 API，并同步 OpenAPI JSON 与 TypeScript 类型。
2. 状态链为 `UPLOADED → VALIDATING → READY/PARTIAL_FAILED/FAILED → APPROVED → EXECUTING → COMPLETED → RECONCILED/ROLLED_BACK`；所有状态写均保留事件和审计。
3. 创建/校验要求 `migration:import`，查询要求 `migration:read`，审批/执行/对账/回滚要求 `migration:write`；审批与回滚额外要求 `PLATFORM_ADMIN`，每个接口重验项目范围。
4. 校验覆盖必填项、枚举、数字/日期、面积、生产编码冲突、批内来源重复、引用完整性和重复有效关系；依赖未通过时下游行一并隔离。
5. 审批部分失败批次必须显式确认；执行在单事务内按项目映射、楼栋、房屋/明细、客户、关系排序，重复执行返回 `replayed=true`。
6. 对账覆盖 5 类对象数量、建筑面积、可用面积、孤儿关系和重复有效关系共 9 项；全部一致才进入 `RECONCILED`。
7. 回滚凭证错误返回 409；正确凭证逆序删除本批新增对象，映射标记失效，Raw/隔离/规范/暂存/映射/变更/事件/审计证据不删除，重复回滚安全重放。

### 批次 3：页面 17 与首轮演练

1. `/system/migrations` 从合成 `CapabilityWorkspaceView` 切换到专用 `MigrationCenterView`，接入真实批次查询和状态。
2. 页面支持上传 JSON、下载字段模板、载入“32 条合格 + 1 条错误”验收样本、五层计数、隔离错误、对象映射、对账表、状态轨迹、审批、执行和显式凭证回滚。
3. 浏览器验收样本包含 1 项目、1 楼栋、10 房屋、10 客户、10 OWNER 关系和 1 条无效关系。校验后 Raw=33、Quarantine=1、Canonical/Staging=32；执行后项目映射=1、生产来源记录=31、反向变更=41。
4. 同一创建请求只保留 33 条 Raw；执行重放不增量；9 项对账全部 `MATCHED`；回滚后活动映射、未回滚变更和迁移目标剩余数均为 0。
5. `StatusTag` 补齐迁移/审批/对账状态；页面目录与验收卡同步标记 G4 实现边界。

### G4 最终门禁证据

| 门禁 | 结果 |
|---|---|
| 后端宿主机 `clean verify` | 23/23，0 失败、0 错误、0 跳过；IAM/项目隔离集成 6/6，真实 MySQL 空库迁移 1/1 |
| Flyway 空库 | 两个独立 MySQL 8 测试库均从空库完成 V1—V12；65 张表、65 个权限 |
| 前端单元/组件/目录/契约 | Vitest 8 个文件、23/23；49 页验收卡一致；提交版、生成类型和运行中 OpenAPI 一致 |
| 类型与生产构建 | Vue/TypeScript 通过；Vite 1666 个模块构建成功 |
| Playwright 全量 | 5 worker 14/14；含 49 页烟雾、153 张基线、权限/IAM、产权和五层迁移生命周期 |
| 迁移主旅程重复 | 独立执行 + 全量并发执行 2/2；每次生产写入后均完成对账和回滚 |
| 视觉 | 页面 17 三个视口受审更新，1440×900 人工检查；147/147 纯比对通过 |
| 依赖安全 | `npm audit --audit-level=high`：0 个漏洞 |
| Docker 与数据卫生 | API/MySQL/Redis healthy、Web 200、运行库 V12/65 表；活动迁移映射=0、未回滚变更=0、跨项目/重复有效关系/非法面积=0 |

49 页视觉集合摘要：1366×768 为 `499b1a41e10f8d4179f5f5a7beb52d87c4749faccdf738cbcd652204683ad156`，1440×900 为 `f312f6d520717b09c802b025cefd55ebd6ee630af5eca4ffb7200980c727e26f`，1920×1080 为 `1f7a7693538986f25df453736e64daea23f76b283f6eabf67defbb8d12b9aaa3`。计算口径见 `docs/visual-baselines.md`。

### G4 阶段判断

G4 的四个退出条件已经满足：同一批次可安全重放；错误行只进入隔离层且不会污染生产表；导入后的数量、关系、建筑面积和可用面积全部一致；41 条生产变更可完整逆序回滚。G4 可以通过阶段退出并进入 G5 费用配置与应收深化。

这只表示结构等价合成样本的受控迁移管线完成。正式目标历史数据仍需要数据所有方批准、字段映射签字、加密导出包、恢复窗口和财务期初对账；G5—G10 及完整 Goal 仍未完成。

## G5：费用配置与应收

执行日期：2026-08-25

实现检查点：`e56ce9f`

### 批次 1：V13/V14 费用与应收治理模型

1. V13 扩展费用定义的税务分类/税率、币种、精度、舍入和临时费用开关，扩展标准的项目范围、计算基础和分摊规则，扩展版本的金额上下限、发布人和发布时间。
2. 分配支持 ASSET/METER 目标、来源、系数、生效区间和取消原因，并增加项目复合外键、目标一致性、唯一键和日期约束。
3. 新增配置事件、应收逐行结果和任务对账三张表；任务保存类型、请求哈希/JSON、请求/生成/跳过/错误计数、金额、版本和状态时间线。
4. 周期账单增加任务引用、账单类型、收费日期、完整配置 SHA-256 和项目—资产—账期唯一键；账单明细增加分配引用。
5. V14 新增明确标识为合成的临时服务费定义，使临时应收页面在全新库可验收。空库迁移从 V1—V14 得到 68 张表、23 个费用定义、16 个标准和 743 条分配。

### 批次 2：类型化配置 API 与计算边界

1. 将原单体 `FeeService` 拆分为 `FeeConfigurationService`、`ReceivableService`、`ReceivableJobProcessor` 和受控异步配置，公共请求/响应集中到 `FeeModels`。
2. 定义支持查询、新建和按版本更新；标准支持创建、版本列表/追加和停用；分配支持查询、无写入预览、批量写入/重放和按日期取消。
3. 同一标准 ACTIVE 版本和同一标准/目标 ACTIVE 分配的有效期不得重叠；边界日期采用闭区间。取消通过截断结束日保留历史。
4. 周期试算支持建筑面积、可用面积、固定金额和仪表用量，按系数、上下限、定义级 0—2 位和四种舍入计算，并返回逐项完整配置快照及整批 SHA-256。
5. 周期/临时任务使用请求键 + 规范化请求哈希；同键同请求返回原任务，同键不同请求 409。异步处理逐行写账单、明细、状态和错误，并生成数量/账单/金额三项对账。
6. 历史账单引用标准版本、分配和配置快照；测试确认定义更新后历史快照及金额不变。不同配置不能覆盖同资产/账期既有账单，失败任务保留冲突证据。

### 批次 3：页面 8—11 与完整生命周期

1. 新增 `FeeConfigurationView`，页面 8 展示/维护定义与标准版本，页面 9 实现先预览后分配和有效期取消。
2. 重构 `ReceivableWorkflowView`，页面 10 以“试算—入队—轮询—逐行证据—自动对账”呈现周期应收。
3. 新增 `TemporaryReceivableView`，页面 11 使用独立临时费用明细、金额快照和异步生成任务；只有允许临时使用的启用定义可选。
4. 增加组件测试和浏览器主旅程；OpenAPI JSON、TypeScript 生成类型和运行中契约同步。
5. 视觉测试将历史应收任务列表固定为空响应，真实任务状态由专用 E2E 验证。页面 8—11 共更新 11 张三视口图片，147/147 纯比对通过。
6. 全量 Playwright 初次并行执行暴露共享持久后端的迁移瞬时写入污染；套件固定为 1 worker 后 15/15 通过。此修改收紧测试隔离，不放宽业务断言。

### G5 最终门禁证据

| 门禁 | 结果 |
|---|---|
| 后端宿主机 `verify` | 24/24，0 失败、0 错误、0 跳过；费用 1/1、IAM/项目隔离 6/6、独立空库迁移 1/1 使用真实 MySQL |
| Flyway 空库 | 三条真实 MySQL 测试路径均从空库完成 V1—V14；68 张表、65 个权限 |
| 费用边界 | 版本重叠、8/9 月切换、分配预览/写入/重放/冲突/取消、HALF_EVEN、历史快照、临时应收全部通过 |
| 前端单元/目录/契约 | Vitest 9 个文件、26/26；49 页验收卡一致；提交版、生成类型和运行中 OpenAPI 一致 |
| 类型与生产构建 | Vue/TypeScript 通过；Vite 1673 个模块构建成功 |
| Playwright 全量 | 单 worker 15/15；包含 G5 周期/临时应收、49 页烟雾、153 张基线及既有 IAM/产权/迁移生命周期 |
| 视觉 | 页面 8—11 共 11 张受审变化；三视口页面级无横向溢出，147/147 纯比对通过 |
| 依赖安全 | `npm audit --audit-level=high`：0 个漏洞 |
| Docker 与数据卫生 | API/MySQL/Redis healthy、Web 200、运行库 V14/68 表；版本/分配重叠、周期账单重复、无效校验值、完成任务对账差异和费用孤儿均为 0 |

49 页视觉集合摘要：1366×768 为 `4e5aa50c58d84e3f4bb1f7eb5eefc12c7aa16cf6799864ae3285277f8fe10d12`，1440×900 为 `245560602846e427933d4228aa6ed789aa21c815622e5b0b5a593348dd67b049`，1920×1080 为 `0782a6ce4f8aea4f189a4103a55f98d309bd12c14206e300da2733afc6407ba9`。完整证据见 `G5费用配置与应收阶段验收-2026-08-25.md`。

### G5 阶段判断

费用标准和分配可重复初始化；重复请求不会重复生成应收；边界日期、舍入和版本切换测试通过；COMPLETED 任务的配置—应收—账单对账差异为 0。G5 可以通过阶段退出并进入 G6 收银与不可变账务。

该结果仅代表合成费用与合成账单的内部闭环。真实财税口径、期初账、支付/发票/银行通道仍需外部授权和签字；G6—G10 及完整 Goal 仍未完成。
